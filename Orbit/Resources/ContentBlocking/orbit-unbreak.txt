[Adblock Plus 2.0]
! Title: Orbit Unbreak
! Version: 1
!
! Orbit-maintained exception rules for pages that one of the subscribed
! upstream lists breaks. This list ships inside the app rather than being
! fetched, so it applies on first launch, offline, and never goes stale.
!
! Every entry must be a reproduced false positive with no fix available in
! any upstream unbreak list, and must be scoped as narrowly as the filter
! syntax allows. Nothing here weakens blocking on any site other than the
! one named.
!
! --------------------------------------------------------------------------
! linear.app
!
! EasyPrivacy line 2340 is `/statsig-proxy/*`, with no `domain=` and no
! `$third-party` qualifier, so it matches on any origin. Statsig's proxy
! delivery mode deliberately routes a site's config bootstrap through the
! site's own first-party origin, so that rule blocks linear.app's own
! https://linear.app/statsig-proxy/v1/initialize and /v1/rgstr. linear.app's
! client never falls back to defaults when that call fails, so the app never
! mounts and the tab is blank.
!
! `$first-party` plus the host anchor keeps this to linear.app asking
! linear.app: third-party Statsig endpoints, and /statsig-proxy/ on every
! other site, stay blocked. Resource types are deliberately unrestricted --
! the bootstrap is fetch/XHR but the telemetry leg goes out over sendBeacon.
@@||linear.app/statsig-proxy/$first-party
